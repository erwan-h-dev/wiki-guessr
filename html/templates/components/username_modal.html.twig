<style>
    .username-modal-overlay {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: rgba(0, 0, 0, 0.5);
        display: flex;
        justify-content: center;
        align-items: center;
        z-index: 9999;
    }

    .username-modal {
        background: white;
        padding: 2rem;
        border-radius: var(--radius-md);
        box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        width: 90%;
        max-width: 400px;
        animation: slideIn 0.3s ease-out;
    }

    @keyframes slideIn {
        from {
            opacity: 0;
            transform: translateY(-20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .username-modal .modal-header {
        margin-bottom: 1.5rem;
    }

    .username-modal .modal-header h2 {
        font-size: 24px;
        font-weight: 700;
        color: var(--gray-900);
        margin-bottom: 0.5rem;
    }

    .username-modal .modal-header p {
        font-size: 14px;
        color: var(--gray-600);
        margin: 0;
    }

    .username-modal .modal-body {
        margin-bottom: 1.5rem;
    }

    .username-modal .form-group {
        margin-bottom: 1.5rem;
    }

    .username-modal label {
        display: block;
        margin-bottom: 0.75rem;
        font-weight: 600;
        color: var(--gray-900);
        font-size: 14px;
    }

    .username-modal input[type="text"] {
        width: 100%;
        padding: 0.75rem;
        border: 2px solid var(--gray-300);
        border-radius: var(--radius-sm);
        font-size: 16px;
        font-family: inherit;
        transition: border-color 0.2s;
    }

    .username-modal input[type="text"]:focus {
        outline: none;
        border-color: var(--primary);
    }

    .username-modal .form-hint {
        font-size: 12px;
        color: var(--gray-600);
        margin-top: 0.5rem;
    }

    .username-modal .error-message {
        padding: 1rem;
        background: #fee2e2;
        border: 1px solid #fca5a5;
        border-radius: var(--radius-sm);
        margin-bottom: 1.5rem;
        color: #dc2626;
        font-weight: 500;
        font-size: 14px;
    }

    .username-modal button {
        width: 100%;
        padding: 0.75rem;
        background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
        color: white;
        border: none;
        border-radius: var(--radius-sm);
        font-weight: 600;
        cursor: pointer;
        font-size: 16px;
        font-family: inherit;
        transition: transform 0.1s;
    }

    .username-modal button:hover {
        transform: scale(1.02);
    }

    .username-modal button:active {
        transform: scale(0.98);
    }

    .username-modal button:disabled {
        opacity: 0.7;
        cursor: not-allowed;
    }

    .username-modal .modal-footer {
        padding-top: 1.5rem;
        border-top: 1px solid var(--gray-200);
        font-size: 12px;
        color: var(--gray-600);
    }

    .username-modal .modal-footer strong {
        color: var(--gray-900);
    }
</style>
<div class="username-modal-overlay"
     data-controller="username-modal"
     data-username-modal-show-on-load="{{ show_username_modal ? 'true' : 'false' }}"
     data-visible="{{ show_username_modal ? 'true' : 'false' }}"
>
    <div class="username-modal">
        <div class="modal-header">
            <h2>Bienvenue!</h2>
            <p>Veuillez entrer votre nom de joueur</p>
        </div>

        <div class="modal-body">
            <div
                class="error-message"
                style="display: none;"
                data-username-modal-target="errorMessage"
            ></div>

            <form
                action="{{ path('player_set_username') }}"
                method="POST"
                data-username-modal-target="form"
            >
                <div class="form-group">
                    <label for="usernameInput">Nom de joueur</label>
                    <input
                        type="text"
                        id="usernameInput"
                        name="username"
                        maxlength="50"
                        placeholder="Entrez votre nom"
                        required
                        autofocus
                        data-username-modal-target="input"
                        value="{{ player.username|e }}"
                    >
                    <div class="form-hint">Entre 2 et 50 caractères</div>
                </div>

                <button
                    type="submit"
                    data-username-modal-target="submitBtn"
                >
                    Confirmer
                </button>
            </form>
        </div>

        <div class="modal-footer">
            <strong>Note:</strong> Vous pourrez changer votre nom plus tard en cliquant dessus pendant les parties.
        </div>
    </div>
</div>
